# Private local notes (never for git)
doc/local/

# Folders
ocr_output/
pdf-ocr-markdown/
pdfs_to_process/
pdfs-done/
math/
# Live runtime DBs (restored via `make snapshot-restore` from snapshots/parquet/)
memory/

# Local embedding model artifact (bge-small-en-v1.5 GGUF, ~35MB; fetched via
# the documented download command + sha256 pin in helpers/core/local_embedder.py)
models/
# Local scratch: gzip snapshots + raw *_backup.* copies (NOT for git; the
# tracked, restore-able snapshot lives in snapshots/parquet/)
db-backup/
backup/
output/
venv/
Reports/
Telegram/
findata/**/images
findata/**/*.json
findata/_pending_relations.txt

# AI Config folders
.llxprt/
.gemini/
.kilocode
.qwen
.serena
.agent
.clavix
.opencode
.vibe
.zcode
.chrome-profile
.codemachine/
.agents/
.claude/
.playwright-cli/
skills-lock.json


# Python cache
__pycache__/
*.pyc
*.jar
.pytest_cache/

# Coverage output (make cover)
htmlcov/
.coverage
.coverage.*

# Environment variables
.env
.venv

# Tool settings
coc-settings.json
opencode.json

# Frontend: the built bundle (static/findata.bundle.js*) is committed so the
# Python deploy stays Node-free; only the TypeScript deps are git-ignored.
frontend/node_modules/

# Temporary files
uploads/
output/
verify_notes_report.txt
database_integrity_report.txt

# OS-specific files
.DS_Store
Thumbs.db
uv.lock

# Generated triage queue from extract_relations.py
# Performance benchmark report (generated by make perf)
perf_report.txt
metrics_report.txt
relations_report.txt
# Gate run reports (generated by make qa / integration / advisory)
qa_report.txt
integration_report.txt
advisory_report.txt
# Orchestrator run log (generated by make maint / maint-full)
maint_report.txt
# Visual QA screenshots (web-gui-tester evidence)
gui-test-screenshots/
