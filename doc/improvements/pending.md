# Pending improvements

- **Bump the project venv to DuckDB ≥ 1.5.5** (duckpgq is fully retired —
  see `completed.md` #91/#92 and
  `doc/improvements/archive/duckpgq_retirement.txt`, now COMPLETE). A plain
  `uv sync` is safe; 1.5.5 was validated end-to-end in a throwaway venv.
