{
  "name": "findata-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript source + esbuild build for the FinData frontends: the viewer SPA (findata.bundle.js) and the standalone entity pages (entity.bundle.js). Built output is committed to ../static/ so the Python deploy stays Node-free.",
  "scripts": {
    "build": "esbuild src/findata.ts src/entity.ts --bundle --format=iife --target=es2020 --outdir=../static --entry-names=[name].bundle --sourcemap --log-level=info",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "prettier": "^3.9.6",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "cytoscape": "3.28.1",
    "cytoscape-fcose": "2.2.0"
  }
}
